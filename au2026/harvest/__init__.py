"""AU 2026 harvester — pulls session decks, handouts and recordings from the
Autodesk University site using the operator's own browser login.

Modules:
    links     — pure link classification (tested)
    manifest  — CSV manifest + per-class markdown notes (tested)
    au_harvest — Playwright driver + CLI (runs on the operator's PC)
"""
